/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lh3.googleusercontent.com', 'c4.wallpaperflare.com']
  }
}

module.exports = nextConfig
